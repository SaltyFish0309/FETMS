---
phase: 02-component-dark-mode-coverage
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/teachers/list/ViewModeToggle.tsx
  - frontend/src/components/teachers/list/DataTable.tsx
  - frontend/src/components/teachers/TeacherKanbanBoard.tsx
  - frontend/src/components/kanban/KanbanCard.tsx
autonomous: true

must_haves:
  truths:
    - "ViewModeToggle buttons have proper contrast in dark mode"
    - "DataTable header and rows display correctly in dark mode"
    - "DataTable pinned columns have correct background in dark mode"
    - "Kanban cards are readable in dark mode"
    - "Kanban columns have subtle distinction in dark mode"
  artifacts:
    - path: "frontend/src/components/teachers/list/ViewModeToggle.tsx"
      provides: "Dark-mode-aware view toggle"
      contains: "bg-muted"
    - path: "frontend/src/components/teachers/list/DataTable.tsx"
      provides: "Dark-mode-aware data table"
      contains: "bg-card"
    - path: "frontend/src/components/teachers/TeacherKanbanBoard.tsx"
      provides: "Dark-mode-aware kanban board"
      contains: "bg-card"
  key_links:
    - from: "frontend/src/components/teachers/list/DataTable.tsx"
      to: "CSS variables"
      via: "Tailwind semantic classes"
      pattern: "(bg-card|bg-muted|hover:bg-muted)"
---

<objective>
Fix ViewModeToggle, DataTable, and Kanban components for dark mode by replacing hardcoded colors with semantic tokens.

Purpose: These interactive components have extensive hardcoded slate colors. Tables need proper header/row contrast per user decision (darker header background, hover-only row distinction). Kanban cards need readable styling in dark mode.

Output: Interactive components that work correctly in both light and dark themes with proper contrast and hover states.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-component-dark-mode-coverage/02-CONTEXT.md
@.planning/phases/02-component-dark-mode-coverage/02-RESEARCH.md
@frontend/src/index.css
@frontend/src/components/teachers/list/ViewModeToggle.tsx
@frontend/src/components/teachers/list/DataTable.tsx
@frontend/src/components/teachers/TeacherKanbanBoard.tsx
@frontend/src/components/kanban/KanbanCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ViewModeToggle with semantic colors</name>
  <files>frontend/src/components/teachers/list/ViewModeToggle.tsx</files>
  <action>
Replace hardcoded colors in ViewModeToggle.tsx:

Container:
- `bg-slate-100` -> `bg-muted`
- `border-slate-200` -> `border-border`

Active button:
- `bg-white` -> `bg-card`
- `text-slate-900` -> `text-foreground`
- `hover:bg-white` -> `hover:bg-card`

Inactive button:
- `text-slate-500` -> `text-muted-foreground`
- `hover:text-slate-900` -> `hover:text-foreground`
- `hover:bg-transparent` -> Keep as-is
  </action>
  <verify>
`npm run build --prefix frontend` passes.
`grep -E "bg-slate|text-slate|border-slate" frontend/src/components/teachers/list/ViewModeToggle.tsx` should return empty.
  </verify>
  <done>
ViewModeToggle uses bg-muted for container, bg-card for active state, text-foreground/text-muted-foreground for text states.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update DataTable with semantic colors</name>
  <files>frontend/src/components/teachers/list/DataTable.tsx</files>
  <action>
Replace hardcoded colors in DataTable.tsx:

Table container:
- `bg-white` -> `bg-card`

Table header (per user decision: darker/distinct background):
- `bg-slate-50` -> `bg-muted/50`

Pinned column backgrounds (important for sticky columns):
- Header pinned: `bg-slate-50` -> `bg-muted/50`
- Cell pinned: `bg-white` -> `bg-card`

Row hover (per user decision: hover-only distinction, no zebra):
- `hover:bg-slate-50` -> `hover:bg-muted/50`

Copyable cell hover:
- `hover:bg-slate-100` -> `hover:bg-muted`

Note: The sticky positioning and z-index logic stays the same, only colors change.
  </action>
  <verify>
`npm run build --prefix frontend` passes.
`grep -E "bg-white|bg-slate-[0-9]|hover:bg-slate" frontend/src/components/teachers/list/DataTable.tsx` should return empty.
  </verify>
  <done>
DataTable uses bg-card for container and pinned cells, bg-muted/50 for header and row hover, bg-muted for copyable cell hover.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Kanban components with semantic colors</name>
  <files>
    frontend/src/components/teachers/TeacherKanbanBoard.tsx
    frontend/src/components/kanban/KanbanCard.tsx
  </files>
  <action>
**KanbanCard.tsx (drag placeholder):**

Dragging placeholder:
- `border-slate-300` -> `border-border`
- `bg-slate-50/50` -> `bg-muted/50`

**TeacherKanbanBoard.tsx:**

Drag overlay (active card):
- `bg-white` -> `bg-card`
- `border-blue-200` -> `border-primary/50`
- `text-slate-900` -> `text-foreground`
- `text-slate-500` -> `text-muted-foreground`

Drag overlay (active column):
- `bg-slate-100/80` -> `bg-muted/80`
- `border-slate-200/60` -> `border-border/60`
- `text-slate-700` -> `text-foreground`

Column container:
- `bg-slate-100/50` -> `bg-muted/50`
- `border-slate-200/60` -> `border-border/60`

Column header:
- `text-slate-700` -> `text-foreground`
- `bg-slate-200` (badge) -> `bg-secondary`
- `text-slate-600` (badge) -> `text-secondary-foreground`
- `text-slate-400 hover:text-red-500` (delete) -> `text-muted-foreground hover:text-destructive`

Card styling:
- `bg-white` -> `bg-card`
- `border-slate-200` -> `border-border`
- `hover:border-blue-300` -> `hover:border-primary/50`
- `text-slate-900` -> `text-foreground`
- `text-slate-500` -> `text-muted-foreground`
  </action>
  <verify>
`npm run build --prefix frontend` passes.
`grep -E "bg-white|bg-slate-[0-9]|text-slate-[0-9]|border-slate" frontend/src/components/teachers/TeacherKanbanBoard.tsx frontend/src/components/kanban/KanbanCard.tsx` should return empty.
  </verify>
  <done>
TeacherKanbanBoard and KanbanCard use bg-card for card surfaces, bg-muted for column backgrounds, border-border for borders, text-foreground/text-muted-foreground for text.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build --prefix frontend` passes
2. ViewModeToggle uses semantic color classes
3. DataTable header uses bg-muted/50, rows use hover:bg-muted/50
4. DataTable pinned columns use bg-card
5. Kanban cards use bg-card, columns use bg-muted/50
6. No hardcoded slate colors remain in interactive components
</verification>

<success_criteria>
- ViewModeToggle container uses bg-muted
- ViewModeToggle active button uses bg-card
- DataTable container uses bg-card
- DataTable header uses bg-muted/50 (distinct from rows per user decision)
- DataTable row hover uses bg-muted/50
- DataTable pinned columns use bg-card background
- Kanban cards use bg-card with border-border
- Kanban columns use bg-muted/50 background
- Kanban text uses text-foreground and text-muted-foreground
- All components build without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-dark-mode-coverage/02-05-SUMMARY.md`
</output>
