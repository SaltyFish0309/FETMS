---
phase: 03-i18n-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/tailwind.config.js
  - frontend/src/components/ui/language-toggle.tsx
  - frontend/src/pages/PreferencesSettings.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Language toggle appears in Settings page"
    - "Clicking toggle options calls i18n.changeLanguage"
    - "Active language is highlighted in toggle"
    - "Traditional Chinese characters render with correct fonts"
    - "UI updates immediately upon switch"
  artifacts:
    - path: "frontend/src/components/ui/language-toggle.tsx"
      provides: "LanguageSwitcher component"
  key_links:
    - from: "frontend/src/components/ui/language-toggle.tsx"
      to: "i18next instance"
      via: "useTranslation hook"
---

<objective>
Implement the UI for language switching and configure the font stack to support Traditional Chinese characters properly.

Purpose: Allow users to control the language preference.
Output: Functional language toggle in Settings and updated font stack.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@frontend/src/pages/PreferencesSettings.tsx
@frontend/src/components/ui/theme-toggle.tsx
@frontend/tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Font Stack</name>
  <files>
    frontend/tailwind.config.js
  </files>
  <action>
    Modify `frontend/tailwind.config.js`:
    - Update `fontFamily.heading` to `['Poppins', 'Microsoft JhengHei', 'Heiti TC', 'sans-serif']`.
    - Update `fontFamily.body` to `['Open Sans', 'Microsoft JhengHei', 'Heiti TC', 'sans-serif']`.
    This ensures Chinese characters fall back to system fonts instead of generic sans-serif.
  </action>
  <verify>
    Inspect `frontend/tailwind.config.js` content.
  </verify>
  <done>
    Font stack includes Traditional Chinese fonts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Language Toggle</name>
  <files>
    frontend/src/components/ui/language-toggle.tsx
  </files>
  <action>
    Create `frontend/src/components/ui/language-toggle.tsx`:
    - Import `useTranslation` from 'react-i18next'.
    - Import UI components (DropdownMenu or Select, Button) similar to `theme-toggle.tsx`.
    - Component `LanguageToggle`:
      - Get `{ i18n } = useTranslation()`.
      - Render a dropdown/select.
      - Options: "English" (en), "繁體中文" (zh-TW).
      - On select: `i18n.changeLanguage(value)`.
      - Show current language icon or text.
  </action>
  <verify>
    Component compiles.
    `npm run test` (verify it doesn't break build).
  </verify>
  <done>
    LanguageToggle component exists and uses i18n hook.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate into Settings</name>
  <files>
    frontend/src/pages/PreferencesSettings.tsx
  </files>
  <action>
    Modify `frontend/src/pages/PreferencesSettings.tsx`:
    - Import `LanguageToggle`.
    - Create a new `<Card>` section below "Appearance" titled "Language & Region" (or similar).
    - Or add a row inside the existing structure if it fits better.
    - Render `<LanguageToggle />` with a label description.
  </action>
  <verify>
    Check file content for the new component.
    (Manual visual check in browser recommended after phase).
  </verify>
  <done>
    Language toggle is rendered in the settings page.
  </done>
</task>

</tasks>

<verification>
`grep "LanguageToggle" frontend/src/pages/PreferencesSettings.tsx`
`grep "Microsoft JhengHei" frontend/tailwind.config.js`
</verification>

<success_criteria>
- Font stack updated.
- LanguageToggle created.
- Settings page includes the toggle.
</success_criteria>

<output>
After completion, create `.planning/phases/03-i18n-infrastructure/03-02-SUMMARY.md`
</output>
