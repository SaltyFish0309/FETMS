---
phase: 02-component-dark-mode-coverage
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/kanban/SortableColumn.tsx
  - frontend/src/components/layout/Layout.tsx
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "SortableColumn drag placeholder adapts to dark mode"
    - "Unused Layout.tsx component is removed"
  artifacts:
    - path: "frontend/src/components/kanban/SortableColumn.tsx"
      provides: "Kanban column drag state"
      contains: "bg-muted"
---

<objective>
Fix remaining dark mode gap in Kanban SortableColumn and remove dead code.

Purpose: Ensure Kanban board interaction states are dark mode compatible and clean up unused legacy layout code.
Output: Dark-mode ready SortableColumn and cleaner codebase.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-component-dark-mode-coverage/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SortableColumn dark mode colors</name>
  <files>frontend/src/components/kanban/SortableColumn.tsx</files>
  <action>
    Replace hardcoded slate colors with semantic tokens for dark mode compatibility:
    - Change `border-slate-300` to `border-border`
    - Change `bg-slate-200/50` to `bg-muted/50`
  </action>
  <verify>
    grep -E "bg-muted|border-border" frontend/src/components/kanban/SortableColumn.tsx && grep -v "slate-" frontend/src/components/kanban/SortableColumn.tsx
  </verify>
  <done>
    SortableColumn uses semantic tokens for drag placeholder state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove unused Layout component</name>
  <files>frontend/src/components/layout/Layout.tsx</files>
  <action>
    Verify Layout.tsx is unused (no imports found in previous steps) and delete it.
    If closer inspection reveals usage, fix hardcoded colors instead:
    - bg-white -> bg-background
    - border-slate-200 -> border-border
  </action>
  <verify>
    ls frontend/src/components/layout/Layout.tsx 2>/dev/null && echo "File exists" || echo "File deleted"
  </verify>
  <done>
    Legacy Layout.tsx is removed or fixed.
  </done>
</task>

</tasks>

<verification>
grep -r "slate-" frontend/src/components/kanban/SortableColumn.tsx | wc -l | grep "0"
</verification>

<success_criteria>
- SortableColumn.tsx uses `bg-muted/50` and `border-border`
- No hardcoded slate colors in SortableColumn.tsx
- Layout.tsx is handled (deleted or fixed)
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-dark-mode-coverage/02-11-SUMMARY.md`
</output>
