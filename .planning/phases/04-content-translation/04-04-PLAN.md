---
phase: 04
wave: 4
depends_on: ['04-01']
files_modified:
  - frontend/src/pages/Teachers.tsx
  - frontend/src/pages/TeacherProfile.tsx
  - frontend/src/components/teachers/list/columns.tsx
  - frontend/src/components/teachers/list/DataTableToolbar.tsx
  - frontend/public/locales/en/teachers.json
  - frontend/public/locales/zh-TW/teachers.json
autonomous: true
---

# 04-04: Translate Teachers Page

## Goal
Implement translations for the Teachers management section, including the data table, filters, and profile view.

## Context
This is a complex page with data tables and columns. We need a `teachers` namespace. The column definitions likely need to become a hook to access translations.

## Tasks

<task type="auto">
  <name>Create teachers locale files</name>
  <files>frontend/public/locales/en/teachers.json, frontend/public/locales/zh-TW/teachers.json</files>
  <action>
    Create `frontend/public/locales/en/teachers.json` and `frontend/public/locales/zh-TW/teachers.json`.
    Add keys for:
    - Columns: `columns.name`, `columns.nationality`, `columns.status`, `columns.email`, `columns.actions`
    - Filters: `filters.status`, `filters.searchPlaceholder`
    - Profile: `profile.personalInfo`, `profile.employment`, `profile.documents`
    - Actions: `addTeacher`, `importTeachers`
  </action>
  <verify>
    Read JSON files to confirm keys.
  </verify>
  <done>
    Teachers locale files created.
  </done>
</task>

<task type="auto">
  <name>Translate Teachers list</name>
  <files>frontend/src/pages/Teachers.tsx, frontend/src/components/teachers/list/columns.tsx, frontend/src/components/teachers/list/DataTableToolbar.tsx</files>
  <action>
    Refactor `frontend/src/components/teachers/list/columns.tsx` to export a `useTeacherColumns` hook instead of a static constant, enabling `useTranslation` usage.
    Update `frontend/src/pages/Teachers.tsx` to use this hook.
    Translate `frontend/src/components/teachers/list/DataTableToolbar.tsx`.
  </action>
  <verify>
    Check `columns.tsx` is a hook or function component and uses `t()`.
    Check `Teachers.tsx` imports and uses the hook.
  </verify>
  <done>
    Teacher table columns and toolbar are translated.
  </done>
</task>

<task type="auto">
  <name>Translate Teacher Profile</name>
  <files>frontend/src/pages/TeacherProfile.tsx</files>
  <action>
    Refactor `frontend/src/pages/TeacherProfile.tsx` to translate section headers and field labels using the 'teachers' namespace.
  </action>
  <verify>
    Read `TeacherProfile.tsx` to confirm translation usage.
  </verify>
  <done>
    Teacher profile page translated.
  </done>
</task>

## Verification
- [ ] Table headers (Name, Status, etc.) translated.
- [ ] Filter placeholders translated.
- [ ] "Add Teacher" button translated.

## Must Haves
- Teachers data table headers and controls translated.
