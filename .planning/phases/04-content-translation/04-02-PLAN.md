---
phase: 04
wave: 2
depends_on: ['04-01']
files_modified:
  - frontend/src/pages/Settings.tsx
  - frontend/public/locales/en/settings.json
  - frontend/public/locales/zh-TW/settings.json
autonomous: true
---

# 04-02: Translate Settings Page

## Goal
Implement translations for the Settings page, including all sections, labels, and descriptions.

## Context
The Settings page contains configuration options for the application. It needs its own namespace `settings` to keep translations organized.

## Tasks

<task type="auto">
  <name>Create settings locale files</name>
  <files>frontend/public/locales/en/settings.json, frontend/public/locales/zh-TW/settings.json</files>
  <action>
    Create `frontend/public/locales/en/settings.json` and `frontend/public/locales/zh-TW/settings.json`.
    Add keys for:
    - Sections: `sections.general`, `sections.appearance`, `sections.language`, `sections.notifications`
    - Labels: `labels.theme`, `labels.language`, `labels.fontSize`, `labels.density`
    - Descriptions: Helper text for each setting.
  </action>
  <verify>
    Read the created JSON files to ensure keys exist.
  </verify>
  <done>
    Settings locale files created.
  </done>
</task>

<task type="auto">
  <name>Translate Settings page</name>
  <files>frontend/src/pages/Settings.tsx</files>
  <action>
    Refactor `frontend/src/pages/Settings.tsx` to use the `useTranslation` hook with the 'settings' namespace.
    Replace hardcoded titles and labels with `t('key')`.
  </action>
  <verify>
    Read `frontend/src/pages/Settings.tsx` and confirm no hardcoded English text remains for UI labels.
  </verify>
  <done>
    Settings page is fully translated.
  </done>
</task>

## Verification
- [ ] Settings page title shows "Settings" / "設定".
- [ ] Section headers translated.
- [ ] Option labels translated.

## Must Haves
- Full translation of Settings page structure.
