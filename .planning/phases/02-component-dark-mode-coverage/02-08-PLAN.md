---
phase: 02-component-dark-mode-coverage
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/TeacherProfile.tsx
  - frontend/src/pages/SchoolProfile.tsx
  - frontend/src/pages/Documents.tsx
  - frontend/src/components/teachers/kanban/AddStageDialog.tsx
  - frontend/src/components/projects/ConfirmDeleteDialog.tsx
  - frontend/src/components/projects/CreateProjectDialog.tsx
  - frontend/src/components/projects/EditProjectDialog.tsx
  - frontend/src/components/projects/HardDeleteDialog.tsx
autonomous: true

must_haves:
  truths:
    - "TeacherProfile page displays correctly in dark mode"
    - "SchoolProfile page has proper contrast in dark mode"
    - "Documents page is readable in dark mode"
    - "Project dialogs use semantic colors"
    - "AddStageDialog uses semantic colors"
  artifacts:
    - path: "frontend/src/pages/TeacherProfile.tsx"
      provides: "Dark-mode-aware teacher profile page"
      contains: "text-foreground"
    - path: "frontend/src/pages/SchoolProfile.tsx"
      provides: "Dark-mode-aware school profile page"
      contains: "text-muted-foreground"
    - path: "frontend/src/pages/Documents.tsx"
      provides: "Dark-mode-aware documents page"
      contains: "bg-card"
  key_links:
    - from: "frontend/src/pages/TeacherProfile.tsx"
      to: "CSS variables"
      via: "Tailwind semantic classes"
      pattern: "(bg-card|text-foreground|text-muted-foreground|border-border)"
---

<objective>
Fix profile pages (TeacherProfile, SchoolProfile, Documents) and remaining dialog components for dark mode by replacing hardcoded colors with semantic tokens.

Purpose: Profile pages contain extensive hardcoded slate/white colors for sections, cards, and text. These are high-traffic pages that need polished dark mode support. Remaining dialogs also need semantic color updates.

Output: All profile pages and remaining dialogs render correctly in both light and dark themes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-component-dark-mode-coverage/02-CONTEXT.md
@.planning/phases/02-component-dark-mode-coverage/02-RESEARCH.md
@frontend/src/index.css
@frontend/src/pages/TeacherProfile.tsx
@frontend/src/pages/SchoolProfile.tsx
@frontend/src/pages/Documents.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TeacherProfile page with semantic colors</name>
  <files>frontend/src/pages/TeacherProfile.tsx</files>
  <action>
Apply semantic color replacements throughout TeacherProfile.tsx:

**Page header section:**
- `text-slate-900` -> `text-foreground`
- `text-slate-500` -> `text-muted-foreground`
- Breadcrumb links - keep accent colors, update slate to semantic

**Profile card/hero section:**
- `bg-white` -> `bg-card`
- Avatar border: `border-slate-200` -> `border-border`
- Name: `text-slate-900` -> `text-foreground`
- Role/title: `text-slate-600` -> `text-muted-foreground`
- Contact info icons: `text-slate-400` -> `text-muted-foreground`

**Tab navigation:**
- Active tab styling - check for hardcoded colors
- Inactive tab: `text-slate-500` -> `text-muted-foreground`
- Tab border: `border-slate-200` -> `border-border`

**Section cards (Personal Info, Passport, Work Permit, Contract, etc.):**
- Card background: `bg-white` -> `bg-card`
- Section headers: `text-slate-800` -> `text-foreground`
- Labels: `text-slate-500` -> `text-muted-foreground`
- Values: `text-slate-900` -> `text-foreground`
- Dividers: `border-slate-200` -> `border-border`

**Status badges:**
- Keep semantic status colors (emerald for active, red for expired, etc.)

**Edit buttons/icons:**
- `text-slate-400 hover:text-slate-600` -> `text-muted-foreground hover:text-foreground`

**Empty states:**
- `text-slate-400` -> `text-muted-foreground`
- `bg-slate-50` -> `bg-muted`
  </action>
  <verify>
`npm run build --prefix frontend` passes.
`grep -rE "text-slate-[0-9]|bg-white|bg-slate-[0-9]|border-slate|hover:bg-slate" frontend/src/pages/TeacherProfile.tsx` should return empty or minimal (status colors may remain).
  </verify>
  <done>
TeacherProfile page uses semantic colors: bg-card for cards, text-foreground for headings and values, text-muted-foreground for labels, border-border for dividers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SchoolProfile and Documents pages with semantic colors</name>
  <files>
    frontend/src/pages/SchoolProfile.tsx
    frontend/src/pages/Documents.tsx
  </files>
  <action>
**SchoolProfile.tsx:**

Apply same patterns as TeacherProfile:
- Page header: `text-slate-900` -> `text-foreground`, `text-slate-500` -> `text-muted-foreground`
- Profile card: `bg-white` -> `bg-card`
- School name: `text-slate-900` -> `text-foreground`
- Address/contact: `text-slate-600` -> `text-muted-foreground`
- Section cards: `bg-white` -> `bg-card`
- Labels: `text-slate-500` -> `text-muted-foreground`
- Values: `text-slate-900` -> `text-foreground`
- Borders: `border-slate-200` -> `border-border`
- Teacher list items: `hover:bg-slate-50` -> `hover:bg-muted/50`

**Documents.tsx:**

- Page header: `text-slate-900` -> `text-foreground`
- Document list container: `bg-white` -> `bg-card`
- Document items:
  - `hover:bg-slate-50` -> `hover:bg-muted/50`
  - File name: `text-slate-900` -> `text-foreground`
  - File meta (size, date): `text-slate-500` -> `text-muted-foreground`
  - File type icon backgrounds - keep colorful or use bg-muted
- Upload area/dropzone:
  - `border-slate-300 border-dashed` -> `border-border border-dashed`
  - `bg-slate-50` -> `bg-muted`
  - Upload text: `text-slate-500` -> `text-muted-foreground`
- Empty state:
  - Icon: `text-slate-300` -> `text-muted-foreground/50`
  - Text: `text-slate-500` -> `text-muted-foreground`
  </action>
  <verify>
`npm run build --prefix frontend` passes.
`grep -rE "text-slate-[0-9]|bg-white|bg-slate-[0-9]|border-slate|hover:bg-slate" frontend/src/pages/SchoolProfile.tsx frontend/src/pages/Documents.tsx` should return empty or minimal.
  </verify>
  <done>
SchoolProfile and Documents pages use semantic colors. Cards use bg-card, text uses text-foreground/text-muted-foreground, borders use border-border, hovers use hover:bg-muted/50.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update remaining dialog components with semantic colors</name>
  <files>
    frontend/src/components/teachers/kanban/AddStageDialog.tsx
    frontend/src/components/projects/ConfirmDeleteDialog.tsx
    frontend/src/components/projects/CreateProjectDialog.tsx
    frontend/src/components/projects/EditProjectDialog.tsx
    frontend/src/components/projects/HardDeleteDialog.tsx
  </files>
  <action>
Apply semantic color replacements to all remaining dialog components:

**Common patterns for ALL dialog files:**
- Dialog shell uses Shadcn Dialog (already semantic)
- Focus on CONTENT within dialogs:
  - `text-slate-900` -> `text-foreground`
  - `text-slate-800` -> `text-foreground`
  - `text-slate-700` -> `text-foreground`
  - `text-slate-600` -> `text-muted-foreground`
  - `text-slate-500` -> `text-muted-foreground`
  - `text-slate-400` -> `text-muted-foreground`
  - `bg-white` -> `bg-card`
  - `bg-slate-50` -> `bg-muted`
  - `bg-slate-100` -> `bg-muted`
  - `border-slate-200` -> `border-border`
  - `hover:bg-slate-50` -> `hover:bg-muted/50`

**AddStageDialog.tsx specific:**
- Stage name input - uses Shadcn Input (already semantic)
- Color picker - keep color swatches
- Labels: `text-slate-700` -> `text-foreground`

**ConfirmDeleteDialog.tsx / HardDeleteDialog.tsx specific:**
- Warning/danger styling - keep red/destructive colors
- Description text: `text-slate-600` -> `text-muted-foreground`
- Buttons - use Shadcn variants (already semantic)

**CreateProjectDialog.tsx / EditProjectDialog.tsx specific:**
- Form labels: `text-slate-700` -> `text-foreground`
- Helper text: `text-slate-500` -> `text-muted-foreground`
- Inputs use Shadcn (already semantic)
- Date pickers use Shadcn Calendar (already semantic)
  </action>
  <verify>
`npm run build --prefix frontend` passes.
`grep -rE "text-slate-[0-9]|bg-white|bg-slate-[0-9]|border-slate|hover:bg-slate" frontend/src/components/teachers/kanban/AddStageDialog.tsx frontend/src/components/projects/ConfirmDeleteDialog.tsx frontend/src/components/projects/CreateProjectDialog.tsx frontend/src/components/projects/EditProjectDialog.tsx frontend/src/components/projects/HardDeleteDialog.tsx` should return empty or minimal.
  </verify>
  <done>
All remaining dialog components (AddStage, ConfirmDelete, CreateProject, EditProject, HardDelete) use semantic colors. Form content uses text-foreground for labels, text-muted-foreground for descriptions.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build --prefix frontend` passes
2. TeacherProfile page uses semantic colors throughout
3. SchoolProfile page uses semantic colors throughout
4. Documents page uses semantic colors for list and upload areas
5. All remaining dialogs use semantic colors
6. No hardcoded slate/white colors remain in profile pages
</verification>

<success_criteria>
- TeacherProfile cards use bg-card
- TeacherProfile text hierarchy uses text-foreground and text-muted-foreground
- TeacherProfile borders use border-border
- SchoolProfile matches TeacherProfile semantic patterns
- Documents page list items use hover:bg-muted/50
- Documents upload area uses bg-muted and border-border
- AddStageDialog uses text-foreground for labels
- Project dialogs (Create, Edit, ConfirmDelete, HardDelete) use semantic colors
- All components build without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-dark-mode-coverage/02-08-SUMMARY.md`
</output>
