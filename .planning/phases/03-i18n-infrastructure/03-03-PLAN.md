---
phase: 03-i18n-infrastructure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/language-toggle.tsx
  - frontend/src/components/ui/language-toggle.test.tsx
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Active language is visually distinguished in the toggle dropdown"
  artifacts:
    - path: "frontend/src/components/ui/language-toggle.tsx"
      provides: "Language selection UI with active state"
      contains: "Check"
  key_links:
    - from: "LanguageToggle.tsx"
      to: "i18n.language"
      via: "comparison in render"
---

<objective>
Fix missing active state indication in LanguageToggle component.

Purpose: Users need to know which language is currently selected when opening the menu.
Output: Updated LanguageToggle component with Check icons for the active language.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-i18n-infrastructure/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Add active state to LanguageToggle</name>
  <files>frontend/src/components/ui/language-toggle.tsx, frontend/src/components/ui/language-toggle.test.tsx</files>
  <action>
    1. Create test file `frontend/src/components/ui/language-toggle.test.tsx` if it doesn't exist, verifying that the active language item renders a Check icon.
    2. Modify the LanguageToggle component:
       - Import `Check` icon from `lucide-react`.
       - In the render function, compare `i18n.language` with the item's language code ('en' or 'zh-TW').
       - Conditionally render the `Check` icon (or add a visual indicator class) next to the active language label.
       - Ensure the layout handles the icon gracefully (e.g., using flexbox or absolute positioning for the checkmark) to align with Shadcn UI patterns.
  </action>
  <verify>
    Run `npm test -- frontend/src/components/ui/language-toggle.test.tsx` to ensure the active state is correctly detected and rendered.
  </verify>
  <done>
    Opening the language toggle shows a checkmark next to the currently selected language, confirmed by tests.
  </done>
</task>

</tasks>

<verification>
Manual verification:
1. Open Settings.
2. Click Language Toggle.
3. Verify Check icon appears next to current language.
4. Switch language.
5. Verify Check icon moves to new language.
</verification>

<success_criteria>
- [ ] Active language is clearly visible in the dropdown menu
</success_criteria>

<output>
After completion, create `.planning/phases/03-i18n-infrastructure/03-03-SUMMARY.md`
</output>
