---
phase: 02-component-dark-mode-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/dashboard/CandidateList.tsx
  - frontend/src/components/dashboard/ExpiryWidget.tsx
  - frontend/src/pages/Dashboard.tsx
autonomous: true

must_haves:
  truths:
    - "Candidate list card displays correctly in dark mode"
    - "Expiry widget alerts are readable in dark mode"
    - "Dashboard page headings and text adapt to dark theme"
    - "Empty states have proper contrast in dark mode"
  artifacts:
    - path: "frontend/src/components/dashboard/CandidateList.tsx"
      provides: "Dark-mode-aware candidate list"
      contains: "text-foreground"
    - path: "frontend/src/components/dashboard/ExpiryWidget.tsx"
      provides: "Dark-mode-aware expiry widget"
      contains: "hover:bg-muted"
    - path: "frontend/src/pages/Dashboard.tsx"
      provides: "Dark-mode-aware dashboard page"
      contains: "text-foreground"
  key_links:
    - from: "frontend/src/components/dashboard/CandidateList.tsx"
      to: "CSS variables"
      via: "Tailwind semantic classes"
      pattern: "(text-foreground|bg-muted|text-muted-foreground)"
---

<objective>
Fix CandidateList, ExpiryWidget, and Dashboard page for dark mode by replacing hardcoded colors with semantic tokens.

Purpose: These components contain hardcoded bg-white, text-slate-*, and hover:bg-slate-50 that don't adapt to dark mode. Empty states and interactive elements need proper contrast.

Output: Dashboard sidebar components and page that render correctly in both light and dark themes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-component-dark-mode-coverage/02-CONTEXT.md
@.planning/phases/02-component-dark-mode-coverage/02-RESEARCH.md
@frontend/src/index.css
@frontend/src/components/dashboard/CandidateList.tsx
@frontend/src/components/dashboard/ExpiryWidget.tsx
@frontend/src/pages/Dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CandidateList with semantic colors</name>
  <files>frontend/src/components/dashboard/CandidateList.tsx</files>
  <action>
Replace hardcoded colors in CandidateList.tsx:

Card container:
- `bg-white` -> remove (Card already uses bg-card from Shadcn)
- Keep `border-none shadow-sm`

CardHeader/CardTitle:
- `text-slate-800` -> `text-foreground`
- `text-slate-500` (subtitle) -> `text-muted-foreground`
- Badge `bg-blue-50 text-blue-700` -> Keep as-is (brand color, works in both modes)

Empty state:
- `text-slate-400` -> `text-muted-foreground`
- `bg-slate-50` -> `bg-muted`
- `text-slate-600` -> `text-foreground`

Candidate rows:
- `hover:bg-slate-50` -> `hover:bg-muted/50`
- `text-slate-900` -> `text-foreground`
- `group-hover:text-blue-600` -> Keep (brand accent)
- `border-slate-100` (avatar) -> `border-border`
- `border-slate-200` (badge) -> `border-border`
- `text-slate-500` -> `text-muted-foreground`

Avatar fallback colors:
- `bg-blue-100 text-blue-700` -> Keep (brand colors, work in both modes)
  </action>
  <verify>
`npm run build --prefix frontend` passes.
`grep -E "bg-white|text-slate-[0-9]|border-slate|hover:bg-slate" frontend/src/components/dashboard/CandidateList.tsx` should return empty.
  </verify>
  <done>
CandidateList uses semantic colors throughout: text-foreground for names, text-muted-foreground for secondary text, hover:bg-muted/50 for row hover, bg-muted for empty state background.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ExpiryWidget and Dashboard page with semantic colors</name>
  <files>
    frontend/src/components/dashboard/ExpiryWidget.tsx
    frontend/src/pages/Dashboard.tsx
  </files>
  <action>
**ExpiryWidget.tsx:**

Empty state:
- `text-slate-400` -> `text-muted-foreground`

Alert items:
- `hover:bg-slate-50` -> `hover:bg-muted/50`
- `text-slate-900` -> `text-foreground`
- `text-slate-500` -> `text-muted-foreground`
- `text-blue-600` (rule name) -> Keep (accent color)
- `text-slate-400` (Settings icon) -> `text-muted-foreground`

CardHeader:
- `text-slate-800` -> `text-foreground`

Alert badge:
- `text-red-600 border-red-200 bg-red-50` -> Keep (semantic alert color, works in both modes)

**Dashboard.tsx:**

Page heading:
- `text-slate-800` -> `text-foreground`
- `text-slate-500` -> `text-muted-foreground`

Loading state:
- `text-slate-500` -> `text-muted-foreground`

Analytics Card header:
- `bg-slate-50/50` -> `bg-muted/50`
- `text-slate-800` -> `text-foreground`

Filter badges:
- Keep `bg-white text-blue-700 border-blue-200` (brand colors)
- `text-blue-700` tracking label -> Keep
- `text-blue-600 hover:text-blue-800 hover:bg-blue-50` -> Keep (brand accent)

Note: Brand/accent colors (blue-*) are intentionally kept as they provide sufficient contrast in both modes and maintain brand identity.
  </action>
  <verify>
`npm run build --prefix frontend` passes.
`grep -E "text-slate-[0-9]|bg-slate-50(?!/)" frontend/src/components/dashboard/ExpiryWidget.tsx frontend/src/pages/Dashboard.tsx` should return empty or minimal matches.
  </verify>
  <done>
ExpiryWidget uses semantic colors for text and hover states. Dashboard page headings use text-foreground, subtitles use text-muted-foreground, card headers use bg-muted/50.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build --prefix frontend` passes
2. CandidateList.tsx contains no bg-white or text-slate-* classes
3. ExpiryWidget.tsx uses text-foreground and hover:bg-muted/50
4. Dashboard.tsx page headings use semantic colors
5. All empty states use bg-muted for backgrounds
</verification>

<success_criteria>
- CandidateList displays correctly in dark mode
- CandidateList row hover uses bg-muted/50
- CandidateList empty state has proper contrast
- ExpiryWidget alerts are readable in dark mode
- ExpiryWidget hover states use semantic colors
- Dashboard page title uses text-foreground
- Dashboard subtitle uses text-muted-foreground
- Analytics card header uses bg-muted/50
- No hardcoded slate colors remain in these components
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-dark-mode-coverage/02-03-SUMMARY.md`
</output>
